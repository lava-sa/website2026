# Daily Log — 2026-04-30

## Completed Today

- Fixed frontend product specs leakage: internal `funnel_config` JSON no longer renders in public specifications table.
- Updated admin products list to show quantity in stock badge when status is in stock (e.g. `18 in stock`).
- Updated buy-funnel behavior so users can select multiple products in a funnel step (instead of single select only).
- Added mailing-list capture improvements:
  - required interest category field
  - conditional machine industry field (only for vacuum machine interest)
  - API validation for these fields
  - admin mailing list table/CSV visibility for interest + industry
  - SQL migration `013_mailing_list_interest_fields.sql`
- Janet reliability and UX updates:
  - switched `/api/gemini-token` to create short-lived ephemeral tokens for Live sessions
  - Live connect now uses explicit API version + model response from server
  - added audio device indicator (mic/output labels) in Janet UI
  - increased Janet z-index so popup sits above menu/header
  - updated Janet opening script to ask for visitor first name at start
- Added Huntex logo entry to industry partners section.

## Deployment / Push Notes

- Main branch was pushed earlier today with funnel/admin/spec fixes in commit `b6773bfd`.
- Remaining local changes (including Janet + Huntex + notes) are to be committed and pushed in the next wrap-up commit.

## Backups

- Git commit history is being used as the official backup trail.
- This daily log records scope and decisions for quick restore/reference.

## Outstanding Issues / Next Actions

- Email domain decision:
  - Prefer `lava-sa.com` for verified sending/canonical domain.
  - Keep `.co.za` as legacy with 301 redirect to `.com` once approved.
- Resend:
  - complete `.com` domain verification and sender configuration.
- Twilio/voice missed calls:
  - local ZA number verification flow blocked/slowed by compliance.
  - resume with either Twilio verified local number or alternative provider.
- 301 migration task pending:
  - map `.co.za` sitemap URLs to `.com` equivalents before redirect rollout.

